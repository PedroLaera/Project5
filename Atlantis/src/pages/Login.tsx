import CardLogin from "../components/custom/CardLogin";

export default function LoginPage() {
  return (
    <div className="w-full min-h-screen flex flex-col items-center justify-center bg-gradient-to-b from-zinc-400 via-zinc-600 to-zinc-900 p-4">
      <CardLogin />
    </div>
  );
}
