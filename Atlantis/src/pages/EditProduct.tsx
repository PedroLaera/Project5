import CardEditProduct from "../components/custom/CardEditProduct";
export default function EditProduct() {
  return (
    <div className="mg-full min-h-screen flex flex-col items-center justify-center bg-gradient-to-b from-zinc-400 via-zinc-600 to-zinc-900 p-4">
      <CardEditProduct />
    </div>
  );
}
