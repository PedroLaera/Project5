import CardCreateProducts from "../components/custom/CardCreateProduct";

export default function CreateProduct() {
  return (
    <div className="min-h-screen flex justify-center items-center  bg-gradient-to-b from-zinc-400 via-zinc-600 to-zinc-900 p-4">
      <CardCreateProducts />
    </div>
  );
}
